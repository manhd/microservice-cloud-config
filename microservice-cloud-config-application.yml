spring:
 profiles:
  active: dev
---

server:
 port: 50001
spring:
 profiles:
  application:
   name: microservice-cloud-config-dev
---
server:
 port: 50002
spring:
 profiles:
  application:
   name: microservice-cloud-config-test
